import { useState } from 'react';
import { useAuth } from '../../context/AuthContext.jsx';
import { planningApi } from '../../api/client.js';

const cards = [{ label: 'Active corridor', value: 'NDLS - KKDE', note: 'Northern operations' }, { label: 'Open work', value: '42 tasks', note: '11 critical' }, { label: 'Recommended GATI', value: '18.4 min', note: '12% below Plan B' }, { label: 'Block utilization', value: '86%', note: '+9% this cycle' }];
export function Dashboard() { const { session } = useAuth(); const [message, setMessage] = useState('System ready'); const [plans, setPlans] = useState([]); async function generate() { setMessage('Generating candidate plans...'); try { const response = await planningApi.generate({ corridorId: 'NDLS-KKDE', horizon: { from: '2026-10-01', to: '2026-10-07' }, scenarioCount: 1000, configVersion: 'v1' }); setPlans(response.data.data?.plans || []); setMessage('Candidate plans ready for review'); } catch { setMessage('Planning service is unavailable'); } }
  return <main className="content"><header><div><p className="eyebrow">THURSDAY, 10 SEPTEMBER 2026</p><h1>Good morning, {session?.name || 'Control Officer'}</h1></div><div className="header-user">{session?.role || 'CONTROL_OFFICER'} <span className="avatar">CO</span></div></header><section className="hero"><div><p className="eyebrow">PLANNING WINDOW / 01 - 07 OCT</p><h2>One possession. Three departments. Zero guesswork.</h2><p>Review the latest cross-department maintenance outlook and choose a robust operating plan.</p><button className="primary" onClick={generate}>Generate candidate plans <span>{'->'}</span></button></div><div className="signal"><span className="signal-dot" />All systems operational<p>Last sync 06:42 IST</p></div></section><section className="metric-grid">{cards.map((card) => <article className="metric" key={card.label}><p>{card.label}</p><strong>{card.value}</strong><small>{card.note}</small></article>)}</section><section className="workspace"><article className="panel"><div className="panel-heading"><div><p className="eyebrow">PLAN COMPARISON</p><h3>Candidate plans</h3></div><span className="status">{message}</span></div>{plans.length ? plans.map((plan) => <div className="plan-row" key={plan.id}><div><strong>{plan.id}</strong><small>{plan.assignments.length} scheduled assignments</small></div><span>{plan.solver_status}</span></div>) : <div className="empty"><strong>No run selected</strong><p>Generate a planning run to compare feasible alternatives from CP-SAT.</p></div>}</article><article className="panel"><div className="panel-heading"><div><p className="eyebrow">RISK SIGNALS</p><h3>Operational watch</h3></div></div><div className="watch-row"><span className="watch-icon amber">!</span><div><strong>Rain probability elevated</strong><small>Review tail-risk scenarios before approval</small></div></div><div className="watch-row"><span className="watch-icon green">+</span><div><strong>11 tasks ready to bundle</strong><small>Across Engineering and S&T</small></div></div></article></section></main>;
}
