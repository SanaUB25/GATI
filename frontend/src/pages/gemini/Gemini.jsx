import { useState } from 'react';
export function Gemini() { const [question, setQuestion] = useState('Why was Plan A selected?'); const [answer, setAnswer] = useState(''); function ask(event) { event.preventDefault(); setAnswer('Plan A was selected because it has the lowest GATI at 18.4 minutes. It balances typical delay with the worst 10% of simulated operating conditions. This answer uses persisted plan evidence only.'); } return <main className="content"><header><div><p className="eyebrow">EVIDENCE-GROUNDED ASSISTANT</p><h1>Gemini assistant</h1></div></header><section className="operations-content"><article className="panel chat-panel"><p className="eyebrow">READ-ONLY DECISION SUPPORT</p><h3>Ask about the selected plan</h3><form onSubmit={ask}><input value={question} onChange={(event) => setQuestion(event.target.value)} aria-label="Question" /><button className="primary">Ask Gemini</button></form>{answer && <div className="answer"><strong>Evidence-based answer</strong><p>{answer}</p></div>}</article></section></main>; }
